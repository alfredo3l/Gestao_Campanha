# =====================================================================
# Supabase — projeto "Acto" (qvjpnucpwdrtxfjqicsu) — schema "campanha"
# =====================================================================
# Pegue as chaves em: Supabase Dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://qvjpnucpwdrtxfjqicsu.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Service role — NUNCA expor para o cliente. Use somente em rotas /api e Server Actions.
SUPABASE_SERVICE_ROLE_KEY=

# =====================================================================
# IMPORTANTE — Schema exposto pela API PostgREST
# =====================================================================
# Para que o supabase-js fale com `campanha.*` diretamente, vá em:
#   Dashboard → Project Settings → API → "Exposed schemas"
#   e adicione "campanha" à lista (mantenha "public" também).
# Sem isso, as queries do client retornarão "schema must be one of ...".
